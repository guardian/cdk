---
"@guardian/cdk": minor
---

Add optional `riffRaffProjectName` property to `GuStackProps` representing the name of the Riff-Raff project used to deploy these resources.
If provided, it is used as the value of the `gu:riff-raff-project` tag on all resources.

See also https://github.com/guardian/riffraff-platform.
